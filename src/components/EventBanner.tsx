import React from 'react';

/**
 * EventBanner (disabled)
 *
 * The banner has been intentionally disabled by returning null.
 * This keeps pages that import EventBanner working without showing the event section.
 *
 * To restore the original banner, replace this file with the previous component code
 * or revert the commit in the GitHub file history.
 */
export default function EventBanner(): null {
  return null;
}
